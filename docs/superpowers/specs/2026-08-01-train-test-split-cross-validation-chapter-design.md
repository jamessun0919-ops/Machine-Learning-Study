# 設計方案：訓練/測試切分與交叉驗證 章節

> 日期：2026-08-01（第 18 個工作階段）

## 背景與問題

依 `curriculum.ts` 順序，階段二（方法論基礎）繼「特徵工程與標準化」之後的下一個主題是「訓練/測試切分與交叉驗證」。開發者確認此主題沿用第 17 階段建立的第四種「技巧/技術類」範本（簡介／常見方法／適用情境與限制／常見誤區／學習摘要資訊圖表／互動式操作與演示），**本次不需修改 `chapter_template_guide.md`**（範本已於 1.3 節記錄）。

## 範圍界定

「交叉驗證」本身有許多變體（k-fold、Leave-One-Out、Stratified k-fold 等），本次確認只涵蓋兩類最基礎常用的方法：

1. **Train/Test Split（訓練/測試切分）**
2. **k-fold 交叉驗證**（固定 k=5，不涵蓋 Leave-One-Out、Stratified 等進階變體）

## 章節內文

### 簡介

> 若拿訓練模型時用過的同一批資料來評估模型表現，得到的分數會過度樂觀——模型可能只是「背下」了訓練資料，而非真正學會可類推到新資料的規律。因此在訓練模型之前，必須先把資料切出一部分「留到最後才用」的測試集，只用剩下的訓練集來配適模型，最後才拿測試集檢驗其在未見過資料上的表現。當資料量較小時（例如本站的 50 Startups 僅有 50 筆），單次切分的結果可能因為切法不同而有明顯落差，這時交叉驗證（Cross-Validation）能提供更穩定可靠的評估方式。

### 常見方法

- **Train/Test Split（訓練/測試切分）**：把資料隨機切成兩份不重疊的子集，直接用切好的訓練集配適模型，再用測試集評估一次。常見比例為 70/30、80/20、90/10——訓練集比例越高，模型能學到的資料越多，但用來評估的測試集就越小、評估結果的不確定性也越高。
- **k-fold 交叉驗證（Cross-Validation）**：把資料平均切成 k 份（例如 k=5），輪流讓其中一份當驗證集、其餘 k-1 份當訓練集，總共訓練並驗證 k 次，最後把 k 次的評估結果平均，作為模型表現的最終估計。相較於單次切分，交叉驗證讓每一筆資料都輪流當過訓練與驗證的角色，評估結果較不受「切分方式運氣好壞」影響，尤其適合資料量較小的情況。

### 適用情境與限制

- **Train/Test Split**：資料量夠大、追求快速實驗、運算資源有限（只需訓練一次）時適用。
- **k-fold 交叉驗證**：資料量較小（例如僅 50 筆的 50 Startups）、需要更穩定可靠的評估、能負擔多次訓練運算成本時適用；本站聚焦最常用的 k=5。
- 不論哪種方法，切分前都應先隨機打亂資料，避免原始資料若本身有排序（例如依時間或類別排列）導致訓練/測試集分佈不均。

### 常見誤區

- **用測試集反覆調整模型參數**：測試集只能在所有調參流程結束後、最終評估時使用一次；若在調參過程中反覆查看測試集表現來挑選最佳設定，等於間接把測試集資訊洩漏進模型選擇過程，最終評估結果會過度樂觀。正確做法是另外切出驗證集，或直接用交叉驗證來調參。
- **切分前忘記隨機打亂資料**：未打亂就依序切分，容易讓訓練集與測試集的資料分佈不一致。
- **資料量太小卻只做一次切分**：切一次的結果可能剛好把「難預測」的樣本集中分到某一邊，導致評估結果不穩定，此時應改用交叉驗證。

## 互動元件規劃

新元件 `TrainTestSplitComparison.tsx`：

- **資料**：沿用 `src/data/50-startups.json` 的 50 筆資料，僅用作「50 個離散樣本點」的視覺化對象，不涉及任何特徵數值——用固定的洗牌排列（hardcoded 常數，非每次隨機重排）決定每個樣本的顯示順序，呼應「切分前要先打亂資料」的教學重點，同時確保畫面可重現。
- **視覺形式**：橫向點狀圖（Plotly `scatter`），50 個資料點依洗牌後的順序排成一列，依目前模式與選擇上色。
- **兩層模式切換**：
  1. 頂層模式按鈕：「Train/Test Split」／「k-fold 交叉驗證」。
  2. Split 模式下：3 個比例按鈕（70/30、80/20、90/10），依洗牌順序取前 N% 標記為訓練集（teal）、其餘標記為測試集（橘色）。
  3. k-fold 模式下：5 個按鈕（第 1～5 折），50 筆資料依洗牌順序平分成 5 組（每組 10 筆），選中的那一折標記為驗證集（橘色），其餘 4 折標記為訓練集（teal）。
- **統計量顯示**：圖表下方顯示目前模式下的筆數統計（例如「訓練集：40 筆／測試集：10 筆」或「訓練集：40 筆／驗證集（第 3 折）：10 筆」）。
- **互動限制**：比照第 14、17 階段確立的慣例，關閉 Plotly 內建拖曳縮放與圖例點擊切換，只保留模式/選項切換按鈕。

## 資訊圖表規劃

Excalidraw 手繪風格，延續本站慣例。純概念、不列實際計算數字，版面為 4 個視覺區塊：

1. **簡介卡**：濃縮自章節簡介段落。
2. **常見方法卡**：主視覺為 5 格橫條圖，示意 k-fold 交叉驗證的輪流驗證機制（中間一格標示為「目前驗證」，搭配弧形箭頭示意輪替），比照 CRISP-DM 循環圖的呈現精神，但為線性 5 格而非六邊形循環。
3. **適用情境卡**：Train/Test Split 與 k-fold 交叉驗證的適用情境對比（沿用「適用情境與限制」內容）。
4. **常見誤區卡**：一般淺色卡片，比照既有章節風格。

## 技術規劃

| 檔案 | 動作 |
|---|---|
| `src/components/charts/TrainTestSplitComparison.tsx` | 新增：互動元件，橫向點狀圖 + 兩層模式切換 + 筆數統計顯示 |
| `src/content/chapters/train-test-split-cross-validation.md` | 新增：frontmatter `title: 訓練/測試切分與交叉驗證`、`stage: 方法論基礎`、`category: [方法論基礎]`、`interactiveComponent: train-test-split-comparison`；`summary.image` 指向新 PNG，`summary.formulas`/`summary.keyStats` 設為空陣列；內文依上方四段落 |
| `src/pages/chapters/[slug].astro` | 修改：新增 `interactiveComponent === 'train-test-split-comparison'` 的字面 JSX 渲染分支 |
| `src/config/curriculum.ts` | 修改：「訓練/測試切分與交叉驗證」主題項目補上 `slug: 'train-test-split-cross-validation'` |
| `src/config/chapters.ts` | 修改：`chapterOrder` 插入新章節於特徵工程與標準化與簡單線性回歸之間，調整前後項目的 `prerequisiteSlug`/`nextSlug` |
| `src/config/curriculum.test.ts` | 修改：「恰好五個已建置章節」斷言更新為六個 |
| `docs/specs/assets-src/train-test-split-cross-validation-summary.html` | 新增：Excalidraw 風格資訊圖表來源檔，引用共用 `rough-engine.js` |
| `scripts/render-train-test-split-cross-validation-infographic.ps1` | 新增：比照既有渲染腳本模式的專用渲染腳本 |
| `src/assets/chapters/train-test-split-cross-validation-summary.png` | 新增（渲染輸出） |

**本次不需修改 `docs/specs/chapter_template_guide.md`**——「技巧/技術類」範本已於第 17 階段記錄在 1.3 節，本章僅是套用既有範本，非建立新範本。

## 驗證方式

- `npx astro check`：0 錯誤/0 警告
- `npm run build`：7 頁面成功產出（新增 train-test-split-cross-validation 頁面）
- `npm run test`：既有測試不受影響（`curriculum.test.ts` 需確認新增 `slug` 欄位後仍通過）
- 資訊圖表渲染後視覺檢查（優先 DOM 量測法，若矛盾則改用像素分析＋二分搜尋替代法）
- 瀏覽器實測：新頁面六區塊正確渲染；互動元件兩層模式切換（Split 三比例、CV 五折）正確更新圖表與筆數統計；知識地圖頁面確認新章節項目已可點擊；`chapterOrder` 插入中段後前後鄰居（特徵工程與標準化、簡單線性回歸）的鏈結皆正確更新，既有章節無迴歸

## 風險與注意事項

- `chapterOrder` 插入中間位置會同時修改「特徵工程與標準化」與「簡單線性回歸」兩個既有項目的 `prerequisiteSlug`/`nextSlug`，需注意不要漏改任一側（第 17 階段已示範過此模式）。
- 互動元件的固定洗牌排列須為 hardcoded 常數（例如寫死的 50 個索引排列），不可用執行期隨機亂數，以確保畫面可重現、且 k=5 每折恰好整除為 10 筆。
